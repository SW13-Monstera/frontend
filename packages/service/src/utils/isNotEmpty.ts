export const isNumberNotEmpty = (num: number | null | undefined) =>
  num !== null && num !== undefined;

export const isStringNotEmpty = (str: string | null | undefined) =>
  str !== null && str !== undefined;
