import { style } from '@vanilla-extract/css';

export const pageWrap = style({
  padding: '3rem',
});

export const postListWrap = style({
  display: 'flex',
  flexDirection: 'column',
  gap: '1rem',
  marginTop: '1.5rem',
});
